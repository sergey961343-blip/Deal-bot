# Telegram Бот для расчёта сделок

Минимальный Telegram-бот на Python (aiogram 3.x) для расчёта сделок.

## Возможности

- Расчёт результата деления суммы на курс
- Поддержка различных форматов ввода чисел (с точкой, запятой, пробелами)
- Пошаговый ввод данных через FSM (Finite State Machine)
- Форматированный вывод результата

## Требования

- Python 3.10+
- aiogram 3.15.0
- python-dotenv 1.0.1

## Установка

1. Клонируйте репозиторий или скачайте файлы

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

4. Получите токен бота от [@BotFather](https://t.me/BotFather) в Telegram

5. Добавьте токен в файл `.env`:
```
BOT_TOKEN=your_actual_bot_token
```

## Запуск

```bash
python main.py
```

## Использование

### Команды:

- `/start` — начать новый расчёт
- `/help` — показать справку по формату ввода

### Процесс расчёта:

Бот последовательно запросит 4 параметра:

1. **Курс** (примеры: "76", "76.5", "76,5", "Курс 76р")
2. **Реквизит** (любой текст)
3. **Банк** (любой текст)
4. **Сумма** (примеры: "43800", "43 800")

### Пример диалога:

```
Пользователь: /start
Бот: Введите курс:

Пользователь: 76
Бот: Введите реквизит:

Пользователь: 4276 1234 5678 9012
Бот: Введите банк:

Пользователь: Сбербанк
Бот: Введите сумму:

Пользователь: 43800
Бот: 
✅ Сделка рассчитана
🏦 Банк: Сбербанк
💳 Реквизит: 4276 1234 5678 9012
📈 Курс: 76,000
💰 Сумма: 43 800,000
🧮 43 800,000 / 76,000 = 576,316
```

## Особенности

- **Long polling** — бот работает без необходимости настройки webhook
- **FSM** — отслеживание состояния пользователя для пошагового ввода
- **Гибкий парсинг** — поддержка чисел с пробелами, точками и запятыми
- **Валидация** — проверка корректности ввода на каждом шаге
- **Форматирование** — результат с запятой как десятичным разделителем

## Структура проекта

```
.
├── main.py           # Основной файл с логикой бота
├── requirements.txt  # Зависимости Python
├── .env.example      # Пример файла с переменными окружения
└── README.md         # Этот файл
```

## Технические детали

- Использует FSM (Finite State Machine) для управления состояниями диалога
- Регулярные выражения для извлечения чисел из текста
- MemoryStorage для хранения состояний (данные не сохраняются при перезапуске)
- Автоматическое форматирование чисел с 3 знаками после запятой

## Лицензия

MIT
